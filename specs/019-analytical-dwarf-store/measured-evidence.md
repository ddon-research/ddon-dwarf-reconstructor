# Measured evidence

## Active evidence boundary (2026-08-09)

The promoted durable v1.1 store is
`output/analytical-dwarf/main/store-4236f598acc8f158/manifest.json`. The complete manifest records
2,305 compilation units, 597,338,011 rows, 1,452 closed Zstandard artifacts, and zero parser
diagnostics. The versioned Temp paths and database names below remain historical evidence labels;
they are not the current durable store or service identity.

The active backend is native Doris over direct Parquet. Iceberg/PyIceberg rows are retained as
historical compatibility and performance evidence only, after the runtime/dependency removal.
For current Doris evidence, use the checked-out `D:\Apache-Doris-version-4.x-docs` pages alongside
the CLI. In particular, treat `information_schema.statistics` and
`information_schema.column_statistics` as empty compatibility views and use `SHOW TABLE STATS`,
`SHOW COLUMN STATS`, `SHOW ANALYZE`, `SHOW AUTO ANALYZE`, and `__internal_schema.column_statistics`.
Full season-two generation, standalone bundle validation, and approved MSVC/IDA/Sonar acceptance
remain pending and are not implied by this store record.

Benchmark rows include source identity, producer/schema/configuration identity, runtime and Python
identity, cold/warm state, machine metadata, resource metrics, output-manifest identity, and an
explicit status (`observed`, `partial`, `blocked`, or `not_observed`).

## 2026-08-09 current live-Doris benchmark

The current-data measurement deliberately reused the promoted complete manifest and the live
`dwarf` database. It did not regenerate Parquet, reload Doris, create a database, change the
fourteen-family schema, add a projection/materialized view, or change session settings. The source
identity is `4236f598acc8f15893181455ed195e39dfa4dbfda4eeda8b56fcbd82312c63c0`; the registry is
`complete`, schema `1.1`, and its expected and observed family counts are equal.

| Surface | Status | Current observation | Evidence boundary |
| --- | --- | --- | --- |
| Existing Doris serving validation | `observed` | All fourteen registry family counts match the promoted manifest; all tablets in the read-only sweep are `NORMAL`; 14/14 retained manual `SHOW ANALYZE` jobs are `FINISHED` | Automatic history still contains four earlier memory-limit failures for `attribute`/`name`; those remain historical diagnostics |
| `MtObject` generate | `observed` | Cold 111.694 s, warm 79.477 s, peak RSS 90.2/90.3 MB; output hash `5cd6e1b8939260ad8456d7313b15ee984e111e394c855568c3ae743e91cfde2c` | Existing compatibility control; one header |
| `rLayout` generate | `observed` | Cold 236.416 s, warm 213.274 s, peak RSS 131.9/132.3 MB; output hash `759cf157efc5f9609966a64a8932dd5f9786e0b24f3c473f4e14d8ba8e5e46e9` | Short control; one header |
| `rAIFSM` generate | `observed` | `--full-hierarchy --exhaustive`, 347.064 s, peak RSS 166.2 MB, 11 ordered headers; `rAIFSM.h` hash `0c63ec7c7267e362b344c8b9a45f6a2d850c8d941a93788cd1481661f58649b8` | Heavy current workload; one observed iteration, not averaged or truncated |
| Existing bounded Doris query contract | `observed`, bounded | Definition lookup returned 1,001 `MtObject`, 145 `rLayout`, and 343 `rAIFSM` rows with ordered hashes | `LIMIT 1001`/first-definition behavior; not a complete `rAIFSM` hierarchy benchmark |
| Doris query profiles | `observed`, bounded | `rLayout`: 35 ms total, 5 ms schedule, 145.55 MB/145 rows, 8/8 index tablets; `rAIFSM`: 40 ms, 5 ms, 194.81 MB/343 rows, 8/8 tablets; no spill counter observed | Profiles describe the bounded index lookup; generation hydration remains represented by the separate process measurements |

The external report is
`C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\current-doris-route-20260809\current-doris-benchmark.json`.
The raw and parsed Doris profiles, all fourteen table/column statistic captures, analyze history,
DDL/index metadata, and tablet details are under
`C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\current-doris-route-20260809\doris-state`.
This is the current serving baseline only. It does not authorize a candidate access path or MV:
promotion still requires exact ordered output parity and end-to-end improvement on the heavy
`rAIFSM` generate path.

## 2026-08-09 optimization evaluation decision

The follow-up evaluation used the same complete source-bound manifest and the live Doris 4.1.3
`dwarf` database. It was an actual serving evaluation, not only a harness check. The canonical
schema, `DUPLICATE KEY` models, source-first keys, bucket counts, V2/ZSTD storage, indexes,
replication, and registry were left unchanged. Generation used the existing SQL-cache-enabled
session defaults; consequently the fresh-process samples below are cache-enabled cold-session
controls, not an operating-system storage-cache eviction.

| Surface | Status | Result | Decision boundary |
| --- | --- | --- | --- |
| Canonical control generation | `partial`, observed samples | `MtObject`: 3 cold and 5 warm samples completed; cold p50/p95 `79.50/108.69 s`, warm p50/p95 `78.48/78.49 s`. `rLayout`: 2 cold samples completed at `211.31/238.48 s`; its warm repetitions were not completed. Completed headers had stable hashes. | The planned repetition matrix was stopped after enough evidence to avoid spending another hour on controls; it is not a full confirmatory p95 matrix. |
| Exhaustive `rAIFSM` | `observed`, exploratory | Fresh process `380.834 s`; same-instance warm run `364.254 s`; both published 11 headers and the same `rAIFSM.h` hash `0c63ec7c7267e362b344c8b9a45f6a2d850c8d941a93788cd1481661f58649b8`. The apparent warm improvement is `4.35%` from one pair. | Exactness passed, but the improvement is below the required `10%` gate and is not confirmatory. SQL cache was enabled, so this does not promote cache tuning. |
| Generation query trace for `rLayout` | `partial` | The stopped trace contains `16,181` observations: `7,381` DIE, `4,186` attribute, `3,539` reference, `1,052` unit, `18` line, and `5` index queries. Execute time sums are `63.885 s` attribute, `51.138 s` DIE, `8.207 s` reference, and `5.509 s` unit; fetch time is only `0.009 s`. | FE profile retrieval did not match the executing query IDs, so every retained profile is `partial`. Tracing added more than 5% wall time and is excluded from performance conclusions. The query-count attribution is still sufficient to identify round-trip/N+1 work as the hotspot. |
| Set-based hydration screen | `observed`, query-level | For 512 source/unit-bound DIE keys, one `IN` query returned the same `1,321` attribute rows in `0.1115 s`; 512 sequential queries took `3.8006 s`, a `34.1x` reduction. | This is the pre-implementation query-level result; the end-to-end serving-path confirmation is recorded in the bounded-hydration section below. |
| Source/name auxiliary table | `observed`, rejected | `dwarf_records_opt_name_b4` contains all `35,250,925` name rows on four healthy tablets with exact ordered-result parity. Warm definition p50s were `5.529 ms` (`MtObject`), `1.849 ms` (`rLayout`), and `2.556 ms` (`rAIFSM`), versus canonical `4.78`, `1.81`, and `2.53 ms` screens. | Fewer tablets and lower local storage did not improve the measured hot lookup. It remains an unbound auxiliary artifact; the canonical registry/configuration is unchanged. |
| Selective statistics | `observed` | All 14 requested selective `ANALYZE` jobs reached `Finished` with zero failures after waiting for terminal state. The retained automatic history still contains four historical memory-limit failures on wide `attribute`/`name` analysis. | Keep selective statistics for promoted builds; do not interpret historical automatic failures as current-job failure, but retain them as operational evidence. |

The optimization decision is therefore **keep the canonical Doris physical design and do not
promote a lookup-table, MV, index, bucket, storage-format, or session-setting change from this
run**. The only measured high-value opportunity was set-based/batched hydration (and narrower
projections around it), which required a real generation-path implementation followed by exact
full-hierarchy `rAIFSM` confirmation. That implementation and confirmation were completed in the
follow-up run below. The canonical physical design remains unchanged; the serving-path hydration
algorithm is the optimization that was promoted. Method-target and DIE-offset locator tables were
not justified by the observed trace and remain `not_observed`; index removal, V2/V3, ZSTD/LZ4,
pipeline parallelism, SQL-cache-disabled controls, and Stream Load worker comparisons also remain
`not_observed` rather than being treated as successful or rejected optimizations.

## 2026-08-09 bounded hydration implementation

The source-bound runtime was then changed to batch metadata, attribute, reference-target, and
child-tag hydration in bounded groups of at most 512 keys. The canonical Doris schema and registry
were not changed. All comparable runs used `resources/DDOORBIS.elf` and the complete manifest
above; a relocated copy under `D:/research` was intentionally not used for latency comparison
because its source-bound selection-cache metadata differs.

| Surface | Status | Current observation | Evidence boundary |
| --- | --- | --- | --- |
| `rLayout` optimized serving path | `observed`, exploratory | Two exact short runs completed in `20.464 s` and `20.198 s`; nearest-rank p50/p95 `20.198/20.464 s` (`n=2`). The 18,346-byte header hash remained `759cf157efc5f9609966a64a8932dd5f9786e0b24f3c473f4e14d8ba8e5e46e9`. | The sample is smaller than the planned 3-cold/5-warm matrix; compare with the earlier current-live `239.168 s` process sample only as directional evidence, not as a new controlled baseline. |
| Exhaustive `rAIFSM` optimized serving path | `observed`, exploratory | One fresh run took `32.123 s`; two repeated runs took `31.683 s` and `31.653 s`. Across `n=3`, nearest-rank p50/p95 were `31.683/32.123 s`; all 11 headers and the `rAIFSM.h` hash `0c63ec7c7267e362b344c8b9a45f6a2d850c8d941a93788cd1481661f58649b8` matched the approved bundle. | The repeated warm sample is `n=2`; compare with the earlier `361.004 s` warm process sample as strong but not fully matrixed evidence. |
| Paired optimized `rAIFSM` trace | `partial`, exact output | The traced run took `83.553 s` and published the same 11 headers. It recorded `2,208` observations: `829` DIE, `760` attribute, `556` reference, `41` line, `13` index, and `9` unit queries. Summed execute time was `11.798 s` DIE, `16.048 s` attribute, `4.918 s` reference, `0.635 s` line, `1.215 s` index, and `0.135 s` unit. | Every retained FE profile was `partial` because query IDs did not match; tracing added `160.1%` wall time versus the fresh untraced run, so traced wall time is excluded from performance conclusions and retained only for attribution. |

The bounded hydration change is therefore retained in the canonical serving runtime. It reduces
the measured exhaustive `rAIFSM` process time by about `91.2%` against the earlier `361.004 s`
warm sample while preserving exact ordered header output. The benchmark command remains a reusable
one-shot regression and promotion tool: it is rerun when the generator, source publication, Doris
image/configuration, or an isolated serving variant changes; it is not a continuously running
service. The remaining physical matrix is still `not_observed` and must not be inferred from this
algorithmic improvement.

Raw artifacts are retained outside source control under
`C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\doris-evaluation-20260809`:
the completed `rAIFSM` bundles, partial canonical controls, redacted query trace, candidate-table
state, and query-level batch comparison. The trace is attribution evidence only; its incomplete
profiles and stopped wall time must not be used as benchmark samples.

## 2026-08-08 Windows crash and restart evidence

The Windows Event Log records a kernel bugcheck during the season-two generation window. System
event `1001` (`Microsoft-Windows-WER-SystemErrorReporting`) reports bugcheck `0x0000000A` and a
saved dump at `C:\Windows\Minidump\080826-16265-01.dmp`; event `41`
(`Microsoft-Windows-Kernel-Power`) records an unclean reboot, and event `6008` records the
unexpected previous shutdown. The current shell cannot read the minidump because access is
denied, so the faulting kernel module and causal relationship to the producer remain unestablished.

The sampled Application log contains a separate `YourPhoneAppProxy.exe` fault after reboot and no
generator-specific application error. After confirming that no season-two generator remained,
the full 289-root command was restarted with `PYTHONFAULTHANDLER=1` and `PYTHONUNBUFFERED=1`.
This is environmental crash evidence, not proof of an Arrow or producer defect; the rerun remains
an active, incomplete acceptance run until its output manifest and validation evidence exist.

## 2026-08-07 v28 Acceptance Record

The current source-bound candidate is the full-direct recovery store at
`C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v28-line-header-recovery-20260807\store-4236f598acc8f158\manifest.json`.
The store is complete, uses schema `1.1`, covers 2,305 compilation units, contains 597,338,011
rows across 1,452 closed Parquet artifacts, and reports zero parser diagnostics. Its recovery
status is `already_applied` under the authorized profile. Strict manifest, artifact, row-group,
payload, and source-identity validation passed. The source-bound SHA-256 is
`4236f598acc8f15893181455ed195e39dfa4dbfda4eeda8b56fcbd82312c63c0`.

The normal Parquet runtime loaded the store and resolved `rLayout` to 145 complete items. A fresh
full-hierarchy generation rerun from the immutable ELF was started in external Temp storage, but
was stopped after more than 40 minutes while still processing the first symbol (`rAIFSM`); it
published no header bundle. Header generation and the repository MSVC/Sonar acceptance adapter
therefore remain blocked in this worktree.

Native and Iceberg Doris dry-run plans were generated from the same manifest-declared file set.
The native v1.1 projection was then loaded into `dwarf_full_v28_20260808_r2` with four workers;
all fourteen family counts exactly match the manifest for 597,338,011 rows, the 2,305 source-bound
units are present, representative tablets are `NORMAL`, and all 21 explicit statistics jobs are
`FINISHED` with zero failures. A query-only backend benchmark completed 36 contract queries in
5.514 seconds; `rLayout` returned 145 matches with warm p50/p95 of 4.57/14.50 ms, and `MtObject`
returned 1,001 bounded matches with warm p50/p95 of 17.77/19.11 ms. The corresponding cProfile
and process-sampler artifacts are retained under
`C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\profiled-doris-v28-20260808`.

The repository quality gates passed lint, formatting, actionlint, typing, dependency checks,
structure checks, architecture tests, documentation checks, full tests, coverage, and audit.
The MSVC/Sonar acceptance command is currently blocked before compilation because the referenced
approved validation directory `output/msvc-header-validation-20260801` is absent from the
checkout; the earlier exit-code-0 claim is therefore not independently reproducible in this
worktree. The fresh Doris database is serving evidence only until header acceptance is rerun against
an available approved header bundle.
evidence status. Full-corpus Parquet materialization is the production measurement; JSONL rows are
fixture/audit evidence only.

Permitted statuses are `observed`, `partial`, `blocked`, and `not_observed`. Skipped real-asset, LLVM, or Doris checks must not be represented as successful acceptance evidence.

## Current observations

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| Doris skills/CLI optimization evidence — current v9 serving fixture | `observed`, serving-only | The current `apache/doris-skills` checkout (`aeeb2fe31071918257846b3d58ac09ffa151d4c1`) was applied with the local `doriscli 0.1.2` binary from `apache/doris-cli` commit `e32ef58b48e0ef88c829177f3944574eadade47a`. `auth status --format json` reports connected MySQL/HTTP endpoints, one alive BE, and the Compose Doris 4.1.3 service. CLI tablet detail reports `full_index` as eight NORMAL tablets, 452.8 MiB, skew 1.10. The profiled base `rLayout` lookup is 224 ms with 145 scanned/returned rows, 33,492-byte peak memory, and zero spills; the optional name-candidate projection is 123 ms, one of four tablets, 145 scanned/returned rows, 29,952-byte peak memory, and zero spills. | The evidence supports keeping source-first Duplicate-key families, ZSTD, Bloom/inverted indexes, explicit statistics, and the candidate projection as an opt-in serving comparison. It does not authorize an MV, N-Gram index, bucket rewrite, or session tuning before complete-corpus profiles; v9 Parquet is rejected as canonical because of its independent payload validation failure. |
| Analytical dependency baseline | `observed` | Refreshed root and isolated C: temp Python 3.14.6 environments report `pyarrow==25.0.0`, `pyiceberg==0.11.1`, `pyiceberg-core==0.8.0`, `PyMySQL==1.2.0`, `SQLAlchemy==2.0.51`, and `pyelftools==0.33`; the lock file resolves the same versions | The long real-ELF producer remains isolated in its already-started exact-version environment; fixtures and service checks use the refreshed root environment |
| Arrow Flight SQL evaluation harness | `partial`: preflight/transport `observed`, fallback `observed`, parameterized contract `blocked`, exact parity/performance `partial` | The optional lock group resolves `adbc-driver-manager==1.12.0` and `adbc-driver-flightsql==1.12.0` against `pyarrow==25.0.0`; the explicit group environment imports both ADBC packages successfully and the focused Flight suite passes 19 tests. The first live containers were labeled with the base Compose file plus a temporary `!override` port file, which suppressed Flight host mappings. Recreating FE/BE with the base and Flight overlay published FE `8030`/`8070`/`9030` and BE `8040`/`8050`; preflight v2 observed FE `127.0.0.1:8070`, FE public socket `192.168.178.81:8070`, BE public socket `127.0.0.1:8050`, both exact startup markers, base SHA-256 `ba6a6169e7ad352e635b0fac32fe23e4af8c4073b187a1464a0691681581a127`, overlay SHA-256 `1b328f3a81a480bc97e6af2ad64bd7874d891bfb1f5cd3d5b2c631156f50c51f`, and rendered-config SHA-256 `4b01984a8d404afa56574f0dd1d3ad9d426c428b2ddf4da119624eab333a5c0a`. The complete current reused-only report is `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\analytical-flight\full-fallback-reused-v3\doris-flight-report.json` with ADBC 1.12.0, PyArrow 25.0.0, query timeout 30 s, fetch timeout 300 s, and `WITH_MAX_MSG_SIZE=16 MiB`. The required qmark probe remains `blocked` with `NotSupportedError: NOT_IMPLEMENTED: [FlightSQL] acceptPutPreparedStatementQuery unimplemented (Unimplemented; ExecuteQuery)`; the explicit fallback then renders supported qmark values as checked literals and completes both symbols. The matrix compares 76 common row-mode reports: 54 strict digests match and 22 differ only because PyMySQL returns Doris `BOOLEAN` as Python `int` while Arrow returns Python `bool`; row counts, order, schema, nulls, values, and hydration digests match. For 10,000-row DIE arrays, Flight rows are about 0.182--0.184 s warm versus PyMySQL 0.120 s, while Flight Arrow-table/RecordBatch/reducer modes are about 0.089--0.092/0.089--0.090/0.026--0.028 s; 10,000-row attribute RecordBatch is about 0.264--0.277 s versus PyMySQL rows about 0.266 s. Reused point lookups are about 0.0068--0.0148 s through Flight versus 0.0013--0.0018 s through MySQL. N+1 hydration is 3.22 s versus 2.36 s for `rLayout` and 19.57 s versus 24.65 s for `MtObject`; bounded hydration is 3.14 s versus 2.28 s and 10.29 s versus 8.95 s, respectively. Client reduction versus Doris aggregation is exact for `name_counts` and intentionally non-exact for `child_tag_counts` because the bounded raw input stops at 100,001 rows while the grouped query covers the full bounded unit. The post-hint FE/BE log check found no `DoGet` timeout or connection-refused messages. | Keep this path benchmark-only. The fallback is a diagnostic compatibility path, not a parameterization substitute. Exact Python type parity, FE-local result-location routing, cold-connection p95, Doris scanned/profile/message-size evidence, and the 110% p95/20% Arrow-native acceptance gates remain open. |
| Performance artifact default path | `observed`, corrected | The default raw-profiler root now uses `%TEMP%\\ddon-dwarf-reconstructor\\performance` on Windows; an explicit `DDON_PERFORMANCE_ARTIFACT_DIR` remains supported. A focused path-policy test prevents fallback to durable `%LOCALAPPDATA%`. | Existing profiles outside the new default remain valid historical evidence; future real-asset profiles must use the Temp root or an explicit C: Temp path |
| Typed records, source binding, one-CU fixture traversal, null DIEs, offset `0`, reference resolution | `observed` | Focused analytical tests — 51 passed on the exact analytical environment on 2026-08-06; includes typed round trips for range, location, line, macro, frame, abbreviation, and name families, a guard that forbids cross-CU pyelftools reference navigation, unsigned 64-bit and exact decimal round trips, source-aware Doris DDL assertions, Stream Load behavior including optional concurrent labeled loads, source-bound Doris query chaining, fail-closed store discovery, scalar `DW_FORM_data1` location handling, and unit-bucket partition derivation before normalization | Deterministic fixture; not full-corpus coverage |
| Repository validation loop after analytical boundary refactor | `observed` | `uv run --no-sync just check`, `just test-unit`, `just test`, `just coverage-ci`, and `just audit` all passed on the refreshed analytical environment on 2026-08-06; `just test-unit` passed 544 tests, `just test` and `just coverage-ci` each passed 546 tests, coverage was 81.10% with all named groups above policy, and Prospector reported zero findings | Does not substitute for real-asset, LLVM, or Doris full-corpus acceptance |
| Nested DWARF specification project checks | `observed` | `uv lock --directory tools/dwarf_spec_pipeline --check`, nested `just test` (17 passed, 1 deselected), and nested `just check` all passed on 2026-08-05. Nested `just test-official` ran but skipped its one official-artifact test because `DWARF_SPEC_OFFICIAL=1` was not enabled | Official Docker-generated artifact validation remains not observed in this loop |
| Direct Parquet Zstandard materialization without JSONL | `observed` | Fresh v9 fixture manifest has `producer=pyelftools-0.33-one-pass-direct`, no `records` file, eight populated family-specific typed Parquet files across the fourteen-family schema, 16 rows, one row group per tiny family file, and no polymorphic `payload_json` column. The complete manifest now records closed-file size, modification timestamp, SHA-256, footer/compression, and row-group metadata in addition to the 512 MiB fact and 128 MiB derived row-group targets with 4,096-row Arrow batches | Full-corpus row-group size/throughput still requires real-corpus measurement |
| Parquet partition discovery and query usability | `observed`, corrected | Read-only Arrow probing of the published high-value fixture initially failed when automatic Hive discovery inferred `unit_bucket` as `int32` while the physical column was `int64`. The new explicit `source_id: string`/`unit_bucket: int64` Hive partition schema succeeds, is used by the Parquet runtime and benchmark, and is covered by a regression test. Explicit partitioning also lets unit-scoped filters prune the derived bucket | A generic caller that bypasses the repository adapter must use the same typed Hive partition schema; this is not a full-corpus selectivity result |
| Published-fixture Parquet query sanity | `observed` | With the typed partition schema, exact name, DIE-offset, parent-edge, and `2^64-1` decimal predicates returned the expected rows. Thirty local Arrow repetitions measured p50/p95 of 0.445/0.501 ms for name, 0.469/0.579 ms for DIE offset, 0.475/0.605 ms for parent edges, and 0.481/0.525 ms for the exact decimal predicate. The source-bound `ParquetDwarfStore` query port measured 0.870/1.013 ms p50/p95 for `find_definitions('Thing')` on the same tiny fixture | Tiny fixture only; no claim about full-corpus throughput or memory |
| Unsigned Parquet/Doris physical compatibility | `observed` | A pre-fix Iceberg statistics job failed while casting `11331043433964896261` from a Parquet `UINT64` to Doris `BIGINT`. The corrected deterministic fixture stores `18446744073709551615` as `decimal128(20,0)`; Arrow restored the exact Python integer, Doris native DDL used `LARGEINT`, Doris-over-Iceberg completed all eight table `ANALYZE` jobs, and CLI query `CONCAT('X', raw_value_uint)` returned `X18446744073709551615` with one scanned row and zero spills | High-value fixture proves the type boundary; full-corpus value distribution and native-vs-Iceberg cost remain pending |
| Real-DWARF semantic subset | `partial` | A first-CU pass over `resources/DDOORBIS.elf` on the exact analytical environment emitted 392 DIEs, 1,276 attributes, 659 references, 2,723 locations, 118,294 frames, 42 abbreviations, and 204 names; Parquet counts matched the manifest and the store loaded successfully | One CU only; it is parser compatibility evidence, not complete-ELF coverage |
| Bounded real-ELF publication metadata | `partial` | The current-code repeat `artifacts materialize-dwarf resources/DDOORBIS.elf --output-dir C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\bounded-real-v10-20260806 --max-cus 1` published a source-bound partial manifest with one CU, 392 DIEs, 1,276 attributes, 659 references, 68 locations, 204 names, 42 abbreviations, 206 derived index rows, 95 raw chunks, and 9 sections. It recorded ten closed Zstandard Parquet files, each with one row group; `inspect-dwarf-store --allow-incomplete` verified the files and returned 190 definition names. | One CU only; `--max-cus` is a schema/parser probe and cannot establish CU completeness, generation parity, Iceberg, or Doris acceptance |
| v1.1 schema and already-repaired-source probe — v28 | `partial`, observed | `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\bounded-v28-line-header-recovery-20260807\store-4236f598acc8f158\manifest.json` completed one real CU with 392 DIEs, 1,276 attributes, 659 references, 204 names, 206 index rows, 21 typed line rows, zero parse errors, and 11 closed Zstandard Parquet files. The manifest is schema `1.1`, records `dwarf_recovery.status=already_applied`, and strict payload/footer/hash inspection with `--allow-incomplete` exited 0. The raw `.debug_info` section records the exact current source bytes at all three guarded contexts. | This validates the schema and idempotent recovery boundary only. It is intentionally partial and cannot serve generation, knowledge export, Doris, Iceberg, or CU-completeness evidence |
| Iceberg format-v2 local publication from direct Parquet | `observed` | Fresh v9 direct fixture with `pyarrow==25.0.0`, `pyiceberg==0.11.1`, and `pyiceberg-core==0.8.0`; eight typed Parquet family files, 16 rows, Zstandard compression, and stable Hadoop-compatible `v1.metadata.json`/`version-hint.text` aliases while preserving the logical container URI. The Iceberg tree contains no second Parquet data copy and its publication metadata references the canonical `parquet\\...` files | Local Python publication is observed; compatibility bridge is intentionally explicit |
| Store-backed generation and analytical method lookup without live ELF session | `observed` | Focused generator/parser tests; materialized session uses derived method index, refuses CU-scan fallback, and now disables automatic sibling/environment legacy-dump discovery | Fixture output only; real header parity pending |
| Deterministic header parity across store projections | `observed` | `test_jsonl_and_parquet_stores_generate_byte_identical_headers` generates the same `Thing` header through the JSONL audit adapter and direct typed Parquet adapter; the complete header strings match byte-for-byte | Deterministic `Thing` fixture only; approved real `rLayout` header hash and full-corpus parity remain pending |
| Store-backed line-program compatibility | `observed` | The Parquet and JSONL adapters now reconstruct source-file entries and line state transitions from the typed `line` family; focused tests preserve file indexes, directories, entry order, and extended-command state without reopening the ELF | Full real-asset declaration-file and generated-header parity remains pending |
| Doris SQL/load plan generation | `observed` | `artifacts load-doris --dry-run` path and native/Iceberg plan tests | Does not prove service health, load, `EXPLAIN`, or profiles |
| Docker/Doris service | `observed` | Docker 29.6.2 Linux daemon; pinned Doris 4.1.3 FE/BE services healthy on 2026-08-05; `doriscli auth status --format json` reported MySQL/HTTP connected and one alive BE | Local fixture service only; images are immutable digests and all state is under C:\Users\morph\AppData\Local\Temp. The former analytical MinIO service was unused and is not part of the current Compose contract. |
| Native Doris load from direct Parquet | `observed` | Fresh v9 C: temp direct fixture, 8 Stream Load requests, 16 rows, all responses `Status=Success` with zero filtered rows; Doris 4.1.3 database `dwarf_family_20260805_v9`, current fourteen-family DDL, using the exact analytical environment | Fixture only; no full-corpus load or p95 comparison |
| Revised Doris source-first DDL live validation | `observed`, corrected | The first fresh load attempt was rejected by Doris 4.1.3 because `DUPLICATE KEY` columns were not the leading physical schema columns. The generator now reorders key definitions before payload columns. Fresh database `dwarf_sourcefirst_20260805_b` accepted all fourteen tables; eight high-value Parquet Stream Loads inserted 17 rows with zero filtered rows, and all fourteen explicit `ANALYZE` jobs finished. `SHOW CREATE TABLE` confirmed source-first keys, `HASH(source_id, unit_offset)` distribution, Bloom filters, and inverted name indexes. | Fixture only; the source/name `EXPLAIN` still touched 8/8 tiny index tablets, so full-corpus pruning and p95 impact remain unmeasured |
| Doris schema/index/query evidence | `observed` | Compiled `D:\doris-cli\target\release\doriscli.exe` verified current v9 source/offset-aware `DUPLICATE KEY` family schemas, Bloom columns on source/offset fields, the quoted line-program `column` field, and Nereids `EXPLAIN` predicate handling. A no-cache native name lookup returned one row in 13 ms, scanned one row, and spilled zero operators; the Iceberg lookup returned one row in 105 ms with one file split, two pushed predicates, and runtime-filter partition-pruning timing | Tiny fixture touches all 8 native tablets and has one Iceberg file; it does not prove full-corpus selectivity |
| Doris sort/distribution-key review | `observed` on the high-value fixture, full effect `pending` | `SHOW CREATE TABLE` on the already-loaded v9 fixture exposed the old offset-first keys and `HASH(unit_offset)` distribution; its source/name `EXPLAIN` touched 16/16 tablets even though the profile scanned one row. The revised source-first DDL now loads successfully in fresh database `dwarf_sourcefirst_20260805_b` with `HASH(source_id, unit_offset)` distributions and additional equality Bloom columns for parent/target/name lookups. Its source/name `EXPLAIN` touched 8/8 tiny index tablets, returned one row, and the no-cache profile scanned one row with zero spills; the five-repeat source-bound suite measured warm `find_definitions` p50/p95 of 2.84/19.57 ms | Existing v9 tables retain the old DDL until an explicit replacement database/load; the tiny fixture does not establish full-corpus pruning or a runtime decision |
| Global DIE-offset lookup shape | `partial` | On the existing tiny v9 native fixture, no-cache profiled `source_id + die_offset` returned one row with a 66 ms scan operator and 535 ms total time; adding `unit_offset` returned one row with a 31.6 ms scan operator and 72 ms total time. Both profiles scanned one row and spilled zero operators | Scheduling dominates this tiny service, but a narrow source/DIE locator remains a measured candidate if representative global-offset lookup frequency justifies duplicating the canonical DIE facts |
| In-flight Parquet queryability | `partial` | During the v6 full traversal, a bounded read-only probe observed 232 open Parquet files totaling approximately 1.46 GiB; all 232 `pyarrow.parquet.read_metadata` probes lacked a valid footer, with files still at the 4-byte Parquet magic header. A 10-second process sample showed CPU time increasing from 12,707.0 to 12,714.0 seconds while RSS varied from 48.7 to 51.1 GiB on the 64 GB workstation. No Arrow, Iceberg, or Doris query was attempted against that uncommitted prefix | This is expected from atomic publication and is not coverage evidence. Use the explicit checkpoint mode for future diagnostic runs; normal runtime remains fail-closed |
| Explicit checkpoint snapshots | `observed` on deterministic interruption | `--checkpoint-every-cus N` now rotates Parquet parts at CU boundaries, records the immutable `parquet_files` list in `checkpoint.json`, rejects the snapshot by default, and returns `QueryStatus.PARTIAL` when loaded with `--allow-incomplete`. Three focused tests cover file rotation, frozen snapshot membership, request validation, and preserved interrupted output | No real-ELF checkpoint run has been started; checkpoint writer-rotation overhead and full-corpus file-count impact remain unmeasured |
| Checkpoint query benchmark route | `observed` on deterministic snapshot | `performance benchmark-dwarf-store --store-manifest <checkpoint> --allow-incomplete` loads only the recorded Parquet files, reports `partial`, and marks Iceberg/Doris `not_observed`; the focused test verifies a later Parquet part is excluded | No real-ELF checkpoint benchmark has been started; timings are diagnostic and cannot establish complete coverage or the 110% gate |
| Doris native statistics | `observed` | v9 `artifacts load-doris --analyze` submitted statistics for all fourteen native family tables; filtered `SHOW ANALYZE` evidence reports every v9 job `FINISHED` with zero failures | This is native-table fixture evidence; full-corpus statistics cost and CBO benefit remain pending |
| Doris-over-Iceberg | `observed` | Doris 4.1.3 created the v9 catalog and the corrected high-value catalog, queried canonical source-scoped tables, and completed Iceberg statistics collection on the high-value attribute table. The high-value no-cache profile returned one row, scanned one row, pushed the exact decimal predicate into the file scan, and reported zero spills; the compiled CLI preserved the exact value when forced through string concatenation | Fixture only, with full-corpus query parity and p95 comparison pending |
| Doris optimization benchmark | `observed` | Exact-version v9 reports: native `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\benchmark-v9-native\benchmark-report.json` completed the `Thing` query suite with native wall time 1.050 s and `find_definitions` warm p50/p95 14.2/17.4 ms; Doris-over-Iceberg `benchmark-v9-iceberg\benchmark-report.json` completed the same suite with wall time 0.998 s and 104.3/106.3 ms. Direct Parquet `find_definitions` warm p50 was 0.55 ms and direct Iceberg Python was 5.70 ms | Fixture only; current-runtime/zstd-SQLite baselines and the 110% gate are not observed |
| Benchmark hierarchy and knowledge-export coverage | `observed` on deterministic fixture | The benchmark suite now includes explicit inheritance and field-layout child-tag predicates for store, physical Parquet, and Doris query paths. `--run-knowledge-export` runs each selected root through the store-backed generator and records complete manifest paths plus a deterministic output-tree SHA-256; a direct `Thing` fixture sanity run completed in 89 ms with four output files | No real-asset export hash or baseline comparison has been observed |
| LLVM UCRT64 `--statistics` | `observed` | The documented MSYS2 UCRT64 profile (`C:\msys64\ucrt64.exe`, non-interactive `bash.exe --login -lc`) resolved LLVM 22.1.8 and exited 0. The captured `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\llvm-ucrt-v27-20260807\statistics.txt` reports 671,620,939 `.debug_info` bytes. | Generic LLVM metrics; not a replacement for source-bound row counts |
| LLVM UCRT64 `--verify` and `--verify-json` | `partial` | Both completed with exit 1 against the v27 ELF. `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\llvm-ucrt-v27-20260807\verify.json` reports 24,664 errors: 3,286 parent-range, 48 overlapping-`DW_AT_ranges`, and 21,330 overlapping-address-range diagnostics. | Input contains verifier-visible range defects; records must not be discarded |
| `resources/DDOORBIS.elf` JSONL full materialization | `partial` | Stopped after approximately 7.26 GB of JSONL while evaluating the transport; first 32 CUs produced 10.6 million records, demonstrating JSONL staging is not the production path | Incomplete temp artifact; do not treat as coverage evidence |
| `resources/DDOORBIS.elf` direct Parquet full materialization and output parity — v5 attempt | `blocked` | The preserved run under `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v5-20260805` emitted only incomplete Parquet files and Windows Error Reporting recorded `python.exe` access violation `0xc0000005` in `pyarrow\lib.cp314-win_amd64.pyd` at approximately 29.7 GB RSS; no manifest was published | The old `UINT64` physical schema run is diagnostic only; the partial files are not query or coverage evidence |
| `resources/DDOORBIS.elf` direct Parquet full materialization and output parity — v6 retry | `blocked` | Read-only inspection after the v6 retry found no running producer, no `manifest.json`, and no `checkpoint.json` under `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v6-20260805`. The directory contains 362 Parquet files totaling approximately 2,507,437,703 bytes; the producer stderr contains only ELF-platform detection and DWARF-load events. The files have no published source-bound contract and are not queryable or loadable | The termination cause is not established. The 800 MB ELF and external compressed dump remain explicit inputs; this attempt cannot satisfy completeness, parity, or performance gates |
| `resources/PS3/EBOOT.ELF` pyelftools compatibility | `blocked` | The 351 MB PS3 ELF reports 1,092 DWARF-2 CUs, but pyelftools 0.33 aborts in `.debug_ranges` with `ELFParseError: expected 8, found 0` before an analytical manifest can be published | Retain as an explicit old-toolchain/fixture limitation; LLVM UCRT64 cross-check and PS4 real-asset evidence remain separate |
| Current-runtime baseline and 110% p95/peak-memory comparison | `partial` | The patched benchmark now records an observed source-bound live pyelftools `rLayout` baseline in `baseline-v27-current-runtime-180s-r2-20260807\benchmark-report.json`: 151.102 s cold, 167.795 s warm, 18.16 GB peak RSS, and a complete candidate after 1,534 CUs. | The complete 110% comparison is still open because this is a single-symbol legacy lookup baseline, not the full file query contract, and the approved historical header baseline is unavailable. |

## Post-correction query and Doris evidence (2026-08-05)

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| Real-ELF checkpoint parent/query contract | `partial` | The corrected eight-CU checkpoint at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\real-checkpoint-v9-20260805\store-4236f598acc8f158` has closed, hashed Parquet files and an explicit `in_progress` checkpoint manifest: 8 units, 529,992 DIEs, 1,544,043 attributes, 199,895 names, 200,572 derived index rows, 791,327 references, 77,282 locations, 3,227 line rows, 1,623 ranges, 1,036 abbreviations, 95 raw chunks, and 9 sections. `rLayout` resolves to three depth-1 DIEs under their CUs; `MtObject` method paths resolve at depth 2 under the class DIE. | The snapshot is partial and diagnostic-only. The earlier v8 snapshot predates the null-DIE parent-stack fix and is not parent-parity evidence. |
| Direct Parquet query benchmark after projection/cache changes | `partial` | `benchmark-real-checkpoint-v9-tagcounts-20260805\benchmark-report.json` records 3,349,109 rows in 22 closed Parquet files and 18 query measurements. The post-change `rLayout` definition lookup returned 3 matches with 5.20 ms warm p50 and 5.22 ms warm p95; the complete query contract returned complete results for found operations and explicit `not_found` for absent fixture evidence. | The report is intentionally marked partial because its source-bound store contains only eight CUs; it cannot establish full-corpus parity or the 110% gate. |
| Python method/line CPU evidence | `observed` on partial workload | The same `performance` runner workload was profiled independently with cProfile before and after the changes. The first profile measured approximately 9.73 s and 507 `_rows` calls; CU-aware DIE hydration reduced this to 6.51 s and 229 calls; the final tag-only child projection measured 3.49 s of cProfile CPU (3.71 s process wall), with `parquet_store.py:_rows` at 2.37 s across 110 calls. The final artifact is `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\profile-query-v9-final-20260805\analytical-store-query-v9-final\4e0621b01ed64bc9978cd8b630dc0145\cprofile.prof`. | This is a bounded eight-CU query workload, not the one-pass producer. It supports the Parquet query optimization; it does not predict full-corpus materialization cost. |
| Scalene CPU/memory evidence | `partial` | The matching final Scalene artifact is retained under `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\profile-query-v9-final-20260805`. On Windows/CPython 3.14, Scalene reported process/memory evidence but no non-zero application line attribution after the repository's `scalene_target.py` launcher line was excluded; raw maximum traced footprint was approximately 824.7 MiB and process peak RSS was approximately 1.35 GiB. The normalizer reports unavailable application attribution explicitly instead of ranking the launcher as a hotspot. | cProfile is the usable method/line CPU evidence for this run; Scalene line-level CPU conclusions remain unavailable until a runtime/tool combination produces application-file samples. |
| Doris CLI plan and profile evidence | `observed` on source-first fixture | `D:\doris-cli\target\release\doriscli.exe` (`doriscli 0.1.2`) connected to Doris `4.1.3-rc02`. `SHOW CREATE TABLE` confirmed `DUPLICATE KEY(source_id, unit_offset, die_offset, index_type)`, `HASH(source_id, unit_offset)` with 8 buckets, Bloom filters on source/offset/name/target fields, and an inverted `name` index. `EXPLAIN` pruned an exact source/CU/DIE lookup to 1/16 tablets; source/name definition lookup touched 8/8 index tablets because CU is not known. Query `34dea657fa24969-a96c9eac68510773` returned one row in 13 ms; the profile reported one scanned row, 415 maximum scan bytes, and no spill. | The database contains only the tiny high-value fixture: 2 index rows and one live backend. Tablet skew and 8/8 name lookup are not production-scale conclusions. No new DDL or materialized view was promoted from this evidence. |
| Doris optimization decision | `observed`, full effect `pending` | The source-first Duplicate-key design is retained as the default for new native tables. Existing indexes, statistics, and profiles were inspected with the compiled CLI before considering changes; no speculative MV, bucket-count, or secondary-index change was applied. | Full-corpus load, tablet distribution, p50/p95 cold/warm profiles, scanned bytes, memory, spills, and generated-output parity are still required before certifying Doris or the file runtime. |
| Native Doris full-v9 optimization recheck | `observed`, serving-only | `D:\doris-cli\target\release\doriscli.exe` (`doriscli 0.1.2`) reports a healthy local backend. In `dwarf_full_v9_20260806`, all 15 tables have `SHOW ANALYZE` state `FINISHED`, all tablets are `NORMAL`, and observed tablet skew is at most 3.0. The compiled CLI rechecked source-first `DUPLICATE KEY`/Bloom/inverted-index DDL, `EXPLAIN`, tablet distribution, and profiles: exact `full_die` source/CU/DIE lookup pruned to 1/16 tablets and returned one row in 14 ms with one scanned row and no spills; base `full_index` `rLayout` lookup returned 145 rows in 224 ms and touched 8/8 tablets; the measured `full_index_name_candidate` projection returned the same ordered 145 rows in 123 ms and touched 1/4 tablets. | The native database was loaded from the rejected v9 Parquet publication whose independent payload audit found a Zstandard page error. These are valid Doris serving/optimizer observations, not canonical store, header-parity, or acceptance evidence. The name-candidate projection remains opt-in until a complete replacement store and broader p50/p95 suite validate its lifecycle benefit. |

| Direct Parquet writer lifecycle correction | `observed`, bounded real-ELF | The 32-CU direct probe at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\real-direct-32-v10-20260805` published a source-bound `partial` manifest with 11,569,961 rows across 12 closed Zstandard files. All 12 Parquet footers and hashes validated. The manifest recorded `max_open_writers=64`, `peak_open_writers=12`, and zero automatic rotations for this prefix. A deterministic one-writer test forced two automatic rotations and validated all three closed footers. | The probe does not establish complete-CU coverage or full-corpus rotation count. The complete v8 run is still in progress and remains the required acceptance measurement. |

| Doris skills/CLI evidence refresh | `observed`, fixture scale | The Apache Doris skills checklist and CLI contract were applied: `doriscli auth status --format json`, `SHOW CREATE TABLE`, `tablet --detail`, `EXPLAIN`, `SHOW INDEX`, a no-cache profiled source/CU/DIE lookup, and `profile get --full`. The source-first table pruned the exact lookup to 1/8 tablets, scanned one row and 354 bytes, returned in 9 ms, and spilled nothing; name-only access still touched all 8 tiny index tablets. | The live database has two index rows and one backend. No bucket, index, MV, or key change is promoted from this fixture. Full-corpus Doris load and profile comparison remain pending. |

| Repository profiler evidence refresh | `observed` with explicit Scalene boundary | The common `PerformanceRunner` ran cProfile and Scalene independently against the same v9 checkpoint query suite. cProfile recorded 3.74 s cumulative workload time and identified `parquet_store.py:_rows` (line 321) at 2.53 s self time across 110 calls. Scalene completed and retained process/memory evidence, but produced no actionable application line attribution after the launcher was excluded; the normalizer reported this explicitly. A current bounded real-ELF Scalene run at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\bounded-real-v10-profile-scalene-20260806\profiles` completed with Scalene 2.3.0 and 147 samples; its only ranked application line was import-time class setup at `zstd_dump_parser.py:36`, not a store/query hotspot. | cProfile is the usable method/line CPU evidence for this query workload. Scalene remains non-actionable for optimization on this Windows/CPython 3.14 combination; no Scalene-derived CPU hotspot is claimed. |

| Direct Parquet producer hot-loop correction | `observed`, bounded real-ELF | The post-change cProfile probe at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\probe-profiles-v11-20260805\runs\real-dwarf-materialization-v11-cprofile\19be97fc648340a6a1fe930aad936b7f\cprofile.prof` processed 8 CUs in 136.00 s and recorded 349,494,871 calls. The former scalar/UTF-8 row-size estimator no longer appears in the hot path; the leading producer costs are normalized row projection (`parquet.py:_row`/`normalize_record`) and Arrow flushes, with `unit_emitter.py:_write_die` and `_write_attributes` above them. | cProfile is method/line attribution under profiler overhead; it identifies the next measured optimization boundary but does not by itself establish full-corpus throughput. |
| Direct Parquet 32-CU throughput and integrity probe after hot-loop correction | `partial`, observed | The common `PerformanceRunner` run `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\probe-process-v11-20260805\real-dwarf-materialization-v11-process\741d48ea59204c4196c907bf012fd48b\manifest.json` measured 237.862 s wall, 229.547 s user CPU, 6.547 s system CPU, 3,263,864,832-byte peak RSS, 3,870,253,056-byte peak VMS, 1,570,172,507 bytes read, and 819,182,582 bytes written. Its 32-CU partial store has 12/12 valid Zstandard Parquet footers; footer row counts exactly equal manifest counts: 32 units, 1,848,322 DIEs, 5,292,495 attributes, 2,741,943 references, 692,794 index rows, 688,767 names, 255,442 locations, 29,059 lines, 16,296 ranges, 4,707 abbreviations, 95 raw chunks, and 9 sections. The writer metrics are `max_open_writers=64`, `peak_open_writers=12`, and zero automatic/checkpoint rotations. | `--max-cus 32` is a schema/throughput probe only. It cannot satisfy complete-CU, header parity, Iceberg, Doris, or 110% acceptance gates. |
| Scalene direct-Parquet producer probe | `partial`, explicit tool boundary | The repository `PerformanceProfiler` ran Scalene 2.3.0 independently over the same 8-CU direct-Parquet workload and retained `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\probe-profiles-v11-20260805\runs\real-dwarf-materialization-v11-scalene\21e61dbd6e8447ed844fe78e4e4b683a\scalene.json`. The child completed DWARF production but exited with Windows `WinError 5` when Scalene still held a handle during the atomic temporary-store rename; the run is therefore partial. The retained line report is dominated by the Scalene launcher/eval boundary and is not used to select a producer optimization. | cProfile and py-spy provide the usable application method/line evidence for this Windows/CPython 3.14 probe. A future Scalene run needs a file-lock-safe publication boundary or a compatible runtime before its application line attribution can be promoted. |

## Full-corpus serving evidence (2026-08-07)

The v27 replacement is the first complete, independently payload-validated store, but it was
published with schema `1.0`. The v1.1 line-header contract now makes v27 historical evidence only:
normal readers and materializers reject its stale complete manifest, while its payload and Doris
measurements remain useful as a recorded pre-v1.1 comparison. The earlier v9 rows below also remain
historical serving evidence because their canonical Parquet payload was rejected by the full
row-group read. v28 is the current complete-store candidate; its full-corpus traversal is complete
and its v1.1 Doris serving evidence is recorded above.

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| Complete direct Parquet store — v27 | `observed`, historical pre-v1.1 | `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v27-family-rotate16-20260806\store-4236f598acc8f158\manifest.json` is `complete`, source-bound to SHA-256 `4236f598acc8f15893181455ed195e39dfa4dbfda4eeda8b56fcbd82312c63c0`, and records one traversal of all 2,305 CUs. It contains 596,944,504 rows across 1,451 closed Parquet files, 10,611 row groups, and 3,987,900,609 artifact bytes. All files use Zstandard; parser errors are zero; recovery is the explicitly authorized source-bound LLVM formal-parameter profile with six substitutions. | This is a historical payload candidate only. Its complete schema `1.0` manifest is stale after the v1.1 line-header freeze and is rejected by normal readers/materializers; it is not current header-parity or 110%-baseline evidence. |
| Complete-store payload read — v27 | `observed` | Strict `artifacts inspect-dwarf-store` validation exited 0. An independent Arrow batch read of all 1,451 files completed in 25.949 seconds, verified 596,944,504 rows and 10,611 row groups, found only `ZSTD` codecs, and reported no payload errors. | Footer/hash validation and independent payload reads are both required; this does not establish generated-output parity. |
| Native Doris full load — v27 | `observed` | `dwarf_full_v27_20260807` loaded all 1,451 files with four workers in 308.649535 seconds. All Stream Load responses were HTTP 200/Doris `Success`, with 596,944,504 loaded rows and zero filtered rows. Exact `SELECT COUNT(*)` counts match every manifest family, including 271,018,245 attributes, 141,365,745 references, 35,682,130 index rows, and 35,250,925 names. | `SHOW DATA` was stale for some tables immediately after load; acceptance uses exact table counts, not that asynchronous display. |
| Native Doris health/statistics — v27 | `observed` | The compiled `D:\doris-cli\target\release\doriscli.exe` reports all 15 v27 tables (14 canonical plus the opt-in candidate) with `NORMAL` tablets and finished explicit statistics jobs with zero failures. Tablet skew is 1.0–3.0; the largest table is `full_attribute` at about 2,591.4 MB with skew 1.2. | This is one local BE and one source; multi-BE placement remains unobserved. |
| Native rLayout serving projection — v27 | `observed`, opt-in | `EXPLAIN` and level-2 CLI profiles show base `full_index` touching 8/8 tablets, 145 scanned/returned rows, 153.72 MB scanned, and 208 ms total in profile `2e03a73d604a48b2-a0f9a70c4e9766c9`; `full_index_name_candidate` touches 1/4 tablets, scans 145 rows/30.38 MB, and completes in 26 ms in profile `6555f9c4a41743e5-a31990506adec302`. Five fresh no-cache runs measured p50 149 ms for base versus 9 ms for the candidate. | The candidate is a separately provisioned serving projection, not a new canonical family. It is enabled only through `DDON_DORIS_DEFINITION_LOOKUP_TABLE` after source identity and ordered-result checks. |
| Native point lookup — v27 | `observed` | `EXPLAIN` and profile `76721b78c2af4712-aba714f2e0333bd0` for source/CU/DIE `full_die` pruned to 1/16 tablets, returned one row, scanned one row, and completed in 18 ms with no spill evidence. | This is a point-access sanity check, not a scan/aggregation benchmark. |
| Native query parity — v27 | `observed` | The base 36-query report and the candidate-table 36-query report have identical ordered query names, source/CU/DIE keys, statuses, and match counts for `rLayout` and `MtObject`; both report only `complete`/`not_found`. | Application output/header parity remains a separate gate. |
| Doris-over-Iceberg — v27 | `observed`, reference backend | PyIceberg format-v2 publication references all 1,451 canonical Parquet files with zero second data copies across 13 source-scoped tables; snapshot row counts total 596,944,504. The Doris-over-Iceberg benchmark has 36-query ordered parity with native Doris, but query wall is 291.340 seconds versus 3.664 seconds native; `rLayout` is about 29.747 seconds cold and 28.525 seconds warm p50. | Keep Iceberg for lakehouse interchange and catalog access. Native Doris is the required low-latency serving backend for this corpus. |
| Generic full-store file-query harness — v27 | `partial`, timeout | `query-full-v27-20260807` was stopped after the 15-minute outer limit; repeated `slow_dependency_resolution` showed the generic suite rescanning complete Parquet facts for every query/iteration and no report was published. No store or manifest was changed. | This is a harness limitation, not a store-integrity failure. Use bounded query probes or Doris for serving benchmarks; do not treat the timeout as complete file-runtime evidence. |
| Live pyelftools `rLayout` baseline — v27 | `observed`, explicit baseline | `baseline-v27-current-runtime-180s-r2-20260807\benchmark-report.json` uses the same source identity and an explicit 180-second search bound. The legacy lookup reached a complete candidate after 1,534 CUs: cold wall 151.102 seconds, 18,163,712,000-byte peak RSS, 750,406,849 bytes read; one warm sample was 167.795 seconds. The patched benchmark records `search_timeout_seconds=180.0` and reports the explicitly requested baseline as observed even when the optional zstd/SQLite baseline is not requested. | This is a single-symbol legacy lookup baseline, not full header generation or the complete analytical query contract. It supplies a valid bounded comparison point, but does not by itself close the 110%-of-baseline gate. |
| Store-backed rLayout header — v27 | `partial`, baseline blocked | `generate-v27-rlayout-20260807` completed in 922.901 seconds and published a 63,167-byte header with SHA-256 `BC7344144BD4C0CAFAC14C69DED87028A1DF58669121CB938FDBC5FB8D4D3149`. The approved comparison file `output\\msvc-header-validation-20260801\\ps4\\rLayout.h` is absent from the checkout, Temp, and recorded validation directory; the remembered prior hash is therefore not independently comparable. The knowledge export's 63,197-byte `reconstructed.hpp` is line-for-line equal after the deliberate include-guard normalization. | Do not claim parity against the missing approved baseline or alter the header to match an unavailable artifact. Recover or regenerate the approved baseline before Gate 3 can pass. |
| Store-backed rLayout determinism — v27 | `observed`, internal parity | A second fresh `generate --dwarf-store` process completed in 932.357 seconds with zero failures. Its 63,167-byte `rLayout.h` is byte-for-byte equal to the first run; both SHA-256 hashes are `bc7344144bd4c0cafac14c69ded87028a1df58669121cb938fdbc5fb8d4d3149`. | This establishes reproducibility for the current source/store/configuration. It is not approval of the unavailable historical header baseline. |
| Store-backed rLayout knowledge export — v27 | `observed`, complete | The corrected run `export-knowledge-v27-rlayout-20260807-r2` completed in 935.1 seconds with `PYTHONFAULTHANDLER=1` and atomically published a source-bound `complete` manifest, 1,155 node rows, 2,078 relationship rows, and `reconstructed.hpp`. The manifest source SHA-256 matches v27; its file hashes are `nodes=2e74a8130f7f8ba5aba28e2186a33aa472560494000297e4f14ed764d25ab0e8`, `relationships=6926052a7955e2ab4b60dbeb58486d8a2cde2b189d01e9dbb1f083d7b4ece1f6`, and `reconstructed=a800b6e2ec5b1a7cafc1998e4f1e0d1b289a4462e812e8f82eaf5c3ed78f6b38`. The export now passes present-DIE classification for primitive, enum, and declaration-only targets while failing closed if a target DIE is absent. | The earlier six diagnostics were a closure-policy bug, not missing rows: the exporter had no store-backed `DIETypeClassifier.requires_resolution` callback and treated transparent terminal types as required aggregate dependencies. The callback is now covered by focused tests; real aggregate closure failures remain diagnostics. |
| Generate CPU/profile evidence — v27 | `partial`, timeout | The canonical `performance profile` run at `profile-generate-rlayout-v27` timed out after 900.141 seconds with peak RSS 3,464,294,400 bytes and cProfile unavailable; it reached dependency resolution/47 classes but did not complete. Prior v24 cProfile/py-spy evidence remains the actionable method/line localization: Parquet flush, typed row projection, attribute projection, and `unit_emitter` methods; Scalene remained dominated by its Windows launcher/profiler thread and was not used to select an optimization. | The timed profile is diagnostic only. No CPU conclusion is drawn from the timeout, and no full-run Scalene line attribution is claimed. |

## Historical full-corpus serving evidence (2026-08-06)

The following rows supersede the earlier fixture-only Doris boundaries. The complete source-bound
store remains the correctness boundary; the database is a serving projection and is never used to
infer CU completeness.

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| Complete direct Parquet store | `rejected` after payload audit | Manifest `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v9-20260805\store-output\store-4236f598acc8f158\manifest.json` is marked `complete`, source SHA-256 is `4236f598acc8f15893181455ed195e39dfa4dbfda4eeda8b56fcbd82312c63c0`, and the one-pass producer traversed 2,305 CUs. It contains 596,944,504 rows across 14 families, 413 closed Zstandard Parquet files, 2,244 row groups, and 3,847,037,024 Parquet bytes. Footer row counts, sizes, hashes, and compression metadata validate, but the new full-payload audit fails on `parquet/attribute/.../unit_bucket=7/part-00000.parquet`, row group 10, with `ZSTD decompression failed: Data corruption detected`; direct `ParquetFile.read_row_group` reproduces the failure in `decoded_value_kind`. | This v9 store is not accepted for header parity, Iceberg acceptance, or a new full benchmark. The 110%-of-approved-runtime gate and byte parity against an approved real baseline remain `not_observed`; a fresh full traversal is deferred until payload validation is proven on the producer path. |
| Native Doris full load and row-count parity | `observed` | Database `dwarf_full_v9_20260806` loaded all 413 Parquet files with four concurrent labeled Stream Loads in 263.547 s. Every load returned `Success`, with 596,944,504 loaded rows and zero filtered rows. CLI counts match the manifest for all 14 families, including 95,540,741 DIEs, 271,018,245 attributes, 141,365,745 references, 35,682,130 derived index rows, and 35,250,925 names. | The database is source-bound by the queried `source_id`; it is not the canonical store. |
| Native Doris table health and statistics | `observed` | All 14 canonical tables are `NORMAL`. The largest table, `full_attribute`, has 16 tablets, 2,591.3 MB, skew 1.2, and 133.6–199.6 MB tablets; `full_reference` has 16 tablets and 799.1 MB; `full_name` has 16 tablets and 884.8 MB; `full_index` has eight tablets, 452.8 MB, and skew 1.1. Explicit `ANALYZE TABLE` jobs for all 14 families finished with zero failures. | This is a single local BE with one source; multi-BE placement and multi-source distribution remain unobserved. |
| Native exact DIE lookup | `observed` | `EXPLAIN` for source/CU/DIE predicates pruned `full_die` to 1/16 tablets. The exact lookup returned one row and the profile reported no spill. The source-first `DUPLICATE KEY` and `HASH(source_id, unit_offset)` layout therefore supports the required CU-scoped access path. | The query is a bounded point lookup, not a scan/aggregation benchmark. |
| Native global definition lookup | `observed` | Base `full_index` `EXPLAIN` touches 8/8 tablets because the query has no CU predicate; the no-cache CLI profile returned 145 `rLayout` rows and scanned 155,105,361.92 bytes. The compiled CLI query suite completed 36 source-bound queries in 0.427 s; warm `find_definitions` p50/p95 was 1.936/2.219 ms for `rLayout` and 4.620/4.730 ms for `MtObject`. | The name-only path is the measured reason for a serving projection; no generic MV or bucket change is inferred. |
| Measured native name serving projection | `observed`, opt-in | Experimental `full_index_name_candidate` contains 35,250,925 definition rows in four `NORMAL` tablets (286.9 MB, skew 1.1) and has complete statistics. Ordered `rLayout` and `MtObject` results exactly match the base index. Post-stat no-cache `rLayout` runs were 7–9 ms against 13–15 ms on the base table; the CLI profile diff isolates the change to the base `OLAP_SCAN_OPERATOR` (13 ms to 6 ms). The full 36-query application suite improved from 0.427 s to 0.412 s, about 3.7%. | The projection is not a new canonical family and is not created by default. Bind `DDON_DORIS_DEFINITION_LOOKUP_TABLE` only to a separately provisioned table whose source identity has been verified; otherwise use `full_index`. |
| Batched Parquet definition hydration | `observed`, correctness-focused | The post-refactor benchmark report `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\benchmark-full-v9-parquet-batched-rlayout-v1-20260806\benchmark-report.json` completed the full Parquet query contract. Direct typed Parquet `find_definitions(rLayout)` returned 145 ordered matches in 0.692 s cold and 0.804 s warm; related CU, DIE, parent, children, inheritance, field-layout, reference, and method queries completed with the expected statuses. Batch hydration now performs one DIE scan and one attribute scan for the matching offsets, while a regression test preserves duplicate index-row multiplicity. | This is not a 110%-baseline result: the report marks the approved baseline `not_observed`, and the broader store-port cold lookup remains 349.532 s under the current process/cache conditions. Existing long-running parity workers predate this edit, so their output cannot validate the new implementation. |
| Profile-led child-count batching | `observed`, improvement accepted for the Parquet adapter | The attached `py-spy 0.4.2` dump/profile of the live pre-refactor generator localized the repeated path to `find_definitions` -> `_candidate` -> `_nested_type_counts` -> `child_tag_counts` -> projected Parquet `_rows`; the sampled thread was idle in the Parquet scan, so the change targets repeated I/O rather than guessed Python arithmetic. The current adapter now primes all missing definition-parent tag counts with one projected DIE scan. Repository benchmark report `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\benchmark-full-v9-parquet-child-batched-v1-20260806\benchmark-report.json` preserves 145 `rLayout` matches and the complete query contract: store-port cold `find_definitions` fell from 349.532 s / 21.327 GB read to 42.985 s / 2.812 GB, warm p50 from 2.145 s to 2.033 s, and cold user CPU from 1,873.5 s to 192.3 s. | Direct typed Parquet `find_definitions` remained in the same sub-second range (0.781 s cold, 0.771 s warm in this run versus 0.692 s/0.804 s previously); this is not a full-corpus generation or 110%-baseline result. The live trace contains only 12 samples over 0.12 s and is localization evidence, not a Scalene CPU hotspot; Scalene application-line attribution remains unavailable on Windows/CPython 3.14. |
| Profile-led CU-scoped child/reference lookup | `observed`, implementation pending benchmark | A current-code `py-spy 0.4.2` trace at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\parity-rLayout-20260806\generate-b-live-30s.speedscope.json` captured 97 samples with the live header stack in `attribute_target` and then `children_for_die` → array/type parsing → class parsing; both reached Parquet `_rows` without a CU predicate. The adapter now adds the known DIE's `unit_offset` to reference, child, and child-tag filters, and focused tests cover the exact predicates. | The active `generate-b` process predates this edit and therefore cannot measure it. Its output is still empty and no post-edit header/runtime improvement is claimed until a fresh process completes with ordered result and header parity. |
| CU-scoped query-contract benchmark | `observed`, positive child-scan evidence | Post-edit report `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\benchmark-full-v9-parquet-cu-scoped-v1-20260806\benchmark-report.json` completed the complete query contract with 145 ordered `rLayout` matches. The CU-known `children` query fell from 6.594 s / 589.6 MB read to 3.633 s / 105.9 MB read relative to the preceding child-batched report; direct Parquet remained sub-second. | The overall cold store-port definition lookup was noisy (61.015 s in this run versus 42.985 s previously) and the benchmark ran beside active parity workers, so only the child predicate/I/O reduction is retained as evidence. Header type/array parity still requires a fresh post-edit generator run. |
| Partition-scoped definition hydration | `partial`, implementation guarded pending replacement store | A fresh current-code direct store query initially reproduced `ZSTD decompression failed: Data corruption detected` when 145 `die_offset` values were sent as one cross-partition attribute `IN` expression. Bucket-scoped hydration reduced the query to 16.819 s with 145 matches; cProfile at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\profile-current-find-v3-20260806.prof` measured 17.856 s, 178 `_rows` calls, 13.489 s in indexed hydration, and 2.531 s in child-count priming, versus 45.973 s, 577 `_rows` calls, 27.255 s, and 15.493 s before batching. The adapter now also retries Zstandard scan failures without Arrow threads and recursively splits CU-scoped attribute batches; focused tests cover bucket grouping, duplicate preservation, and fallback behavior. | The later full payload audit proves that the v9 store contains an unreadable compressed page, so the successful `rLayout` lookup is not global store-integrity evidence. Retain the query optimization, but remeasure it only on a replacement store that passes row-group payload validation; a fresh header run is required for parity. |
| Complete-store payload validation gate | `observed`, v9 rejected | `artifacts inspect-dwarf-store ... --no-verify-source` now performs a full row-group read when artifact verification is requested and fails closed with `Parquet payload is unreadable` at attribute `unit_bucket=7`, row group 10. The same page fails when read directly with PyArrow 25.0.0; the footer and SHA-256 remain unchanged. Publication now validates every closed Parquet row group before declaring a complete projection, while checkpoint rotation keeps its partial/diagnostic status and separate rotation overhead. | The current v9 store and any Iceberg snapshot referencing it cannot satisfy the completion gates. Native Doris row-count/load evidence remains a serving-database observation, not a repair of the canonical corruption. |
| Replacement full Parquet traversal — v12 | `rejected`, native crash | The fresh post-gate run started at 2026-08-06 13:13:54 CEST with direct Zstandard Parquet, no JSONL, and no checkpoint mode. It progressed through unit buckets 0 and 1 and into bucket 2, reaching 6.3 GiB RSS, before Windows Error Reporting recorded `python.exe` access violation `0xc0000005` in `D:\ddon-dwarf-reconstructor\.venv\Lib\site-packages\pyarrow\lib.cp314-win_amd64.pyd` at 13:26:31. No manifest was published; staged Parquet files remain crash artifacts and are not accepted. | Not accepted as a store, query input, or completeness evidence. |
| Native Arrow batch-cap correction | `observed`, bounded tests | The crash trace and py-spy evidence localized the unsafe boundary to large `Table.from_pylist` conversions whose constant-time row estimate undercounted nested DWARF values. The producer now retains the 512 MiB/128 MiB byte targets but hard-caps native conversion inputs at 65,536 fact rows and 32,768 derived rows; the limits are recorded in the Parquet projection manifest. The focused checkpoint/contract suite passed 20 tests after the edit. | A bounded real-ELF rerun must pass before another full traversal is authorized. |
| Bounded real-ELF traversal — v13 | `observed`, partial diagnostic | The post-cap probe started from the same source at 2026-08-06 13:28:23 CEST with `--max-cus 8`, direct Zstandard Parquet, and no JSONL/Iceberg output. It exited 0 after about 49.9 seconds and published an explicitly `partial` manifest after validating all 12 closed Parquet artifacts. The eight-CU slice contains 529,992 DIEs, 1,544,043 attributes, 791,327 references, 200,572 derived index rows, 199,895 names, and 77,282 locations. Row groups obey the native caps: fact families use at most 65,536 rows and the derived index uses at most 32,768; peak open writers were 12 and no rotations occurred. An independent `inspect-dwarf-store --no-verify-source --allow-incomplete` pass also succeeded. | This proves bounded schema serialization and payload integrity after the cap, and authorizes a replacement full traversal. The store remains partial and cannot be used for generation, knowledge export, Doris, or CU-completeness evidence. |
| Replacement full Parquet traversal — v14 | `rejected`, malformed DWARF | The post-cap replacement started at 2026-08-06 13:37:16 CEST and ran for about 33 minutes before pyelftools 0.33 raised `KeyError(2261)` while parsing CU ordinal 349 at unit offset `0x07fdc9b7`; no manifest was published. LLVM 22.1.8 independently reports invalid abbreviation 2261 at the same `.debug_info` region, where the CU table contains only abbreviations 1–260. The private staging directory was removed by the failed atomic materializer cleanup. | This is source-integrity evidence, not a Parquet or schema failure. v14 cannot satisfy CU completeness or query acceptance. The exact malformed-CU fallback must be verified before another full traversal. |
| Targeted malformed-CU fallback probe — v15 preflight | `observed`, partial raw-preserving diagnostic | The targeted real-ELF probe selected CU `0x07fdc9b7` without replaying earlier CUs. It parsed 47,561 valid DIEs, then emitted a partial abbreviation row for code 2261 at byte offset `134394506` with raw prefix `d5119100d2001a838d0000b285020106`, valid abbreviation range 1–260, and `raw_section_preserved=true`; it continued through side-table emission and returned normally. Focused tests and `just check` pass. | The source bytes after the malformed DIE cannot be reconstructed as valid DWARF by pyelftools or LLVM. The full producer must publish the raw section plus explicit parser diagnostics and complete all later CUs; generated-header parity remains required. |
| Live v14 method/line trace | `partial`, diagnostic | While the replacement full traversal was active, `py-spy 0.4.2` recorded `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v14-20260806\pyspy-worker-7904-10s.speedscope.json` for 10 seconds at 50 Hz: 305 samples with 194 Windows stack-read errors. A refresh at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v14-20260806\pyspy-worker-7904-10s-refresh.speedscope.json` captured 151 samples with 348 errors and 7.88 seconds of sampling lag. Across the parseable samples, the leading leaves were `record_sink.py:32` (`write`, 18), `parquet.py:_flush_buffer:181` (17), `parquet.py:write:272` (8), `parquet.py:_row:188` (8), `unit_emitter.py:_write_attributes:172` (7), and Arrow `write_table:1216` (4). | This is current producer localization, not a completed CPU benchmark; the flush/write and Arrow activity is consistent with the deliberate native batch boundary. The high error rate and sampling lag prevent treating this as a complete profile or justifying a second optimization from this sample alone. |
| Live v12 method/line trace | `partial`, diagnostic | A non-blocking `py-spy dump` captured the active producer worker while it was writing a row batch; the retained JSON stack is under `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\full-direct-v12-20260806\pyspy-worker-16684.json`. A parseable 7.28-second speedscope capture contains 364 samples; leaf samples are concentrated in `unit_emitter.py:_value` line 239 (142), `parquet.py:_flush_buffer` line 175 (134), and PyArrow `write_table` (12), with `unit_emitter.py:_write_attributes` also present. A longer attach attempt emitted interpreter-address warnings and timed out, so it is retained as partial evidence only. | This localizes current producer work but is not a benchmark or proof that either line should be optimized; the completed manifest and measured end-to-end comparison remain authoritative. |
| Live Doris routing revalidation | `observed`, externally contended | Fresh `doriscli` `EXPLAIN` shows base `full_index` name lookup at 8/8 tablets versus `full_index_name_candidate` at 1/4 tablets. A paired `profile diff` reported 6,833 ms total for the base query versus 1,670 ms for the candidate and isolated the base-table `OLAP_SCAN_OPERATOR`; both returned the same 145 rows. | The machine was concurrently running the pre-refactor parity workers, so these timings are routing/profile evidence, not a clean latency benchmark. Re-run the no-cache/warm comparison after those workers finish. |
| Validation after payload-integrity gate | `observed` | On 2026-08-06 after the Arrow cap edit, the wider analytical slice passed 51 tests, `just check`, `just test-unit` (545 passed, 7 deselected), `just test` (547 passed, 5 deselected), `just coverage-ci` (81.13% total; 84.8% lines/67.7% branches in the named aggregate groups), `just audit` (zero findings), `just docs-check`, `just analytical-fixture` (51), and Compose `config --quiet`. The source-identity replacement test now forces a timestamp mutation signal so the no-rehash cache contract is deterministic on Windows. The v13 bounded real-ELF store passed an independent incomplete-store inspection. | This validation covers the producer contract and bounded fixtures; it does not substitute for the replacement full-corpus publication, CU completeness, header parity, or backend benchmark. |
| Doris-over-Iceberg full publication and counts | `observed` | Iceberg v2 catalog `dwarf_full_v9_iceberg_20260806` references the same 413 canonical Parquet files through 13 source-scoped tables. The Iceberg publication contains metadata/catalog files but no second Parquet data copy. Counts for every populated family exactly match the complete manifest, and all 13 Iceberg `ANALYZE` jobs finished with zero failures. | Iceberg is the lakehouse/reference layer, not the default serving backend. |
| Doris-over-Iceberg query performance | `observed` | Full Iceberg `rLayout` name lookup opens 382 file splits and scans 90.40 MB in the `FILE_SCAN_OPERATOR`; warm p50/p95 is 2,476.272/2,490.676 ms. `MtObject` warm p50/p95 is 9,669.607/9,744.196 ms. The full 36-query suite takes 81.241 s. Predicates are pushed down, but file-split fanout remains materially slower than native tablet pruning. | The comparison uses the same source-bound query contract and complete Parquet files; it does not reject Iceberg as a canonical interchange format. |
| Doris CLI and repository profiler evidence | `observed` with explicit Scalene boundary | `D:\doris-cli\target\release\doriscli.exe` 0.1.2 captured `SHOW CREATE TABLE`, `SHOW INDEX`, `SHOW COLUMN STATS`, `tablet --detail`, `EXPLAIN`, no-cache profiles, and `profile diff`. The repository `profile-dwarf-store` command was rerun after the query-spec split at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\profiled-doris-query-v3-20260806\profiles`; cProfile traces 36 Doris queries through `benchmark_dwarf_store` → `_doris_measurement` → `doris_queries` → `_doris_symbol_queries` → `run_query_with_metrics`, with 13,170 file reads and 27.36 seconds in the symbol-query layer under the contended profile run. A fresh C:-temp refresh at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\profiled-doris-query-v4-20260806\profiles` recorded cProfile wall 13.807 s, 2.109 s user CPU, 1.766 s system CPU, 64.2 MB process reads, and 5,694 buffered reads; the cProfile method trace is dominated by Doris response reads rather than Python query computation. The subsequent v5 run at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\profiled-doris-query-v5-20260806\benchmark-report.json` recorded the same explicit backend-only boundary: Scalene 2.3.0 retained 1.890 s wall, 1.391 s user CPU, 0.516 s system CPU, 239 MB peak RSS, and 18 samples but no non-zero application-line attribution; cProfile retained 0.746 s wall, 0.453 s user CPU, 0.156 s system CPU, and 7 samples dominated by imports/launcher code because this query-existing workload does not execute producer code. The current bounded real-ELF run also retained a C:-temp Scalene artifact with 147 samples and only import-time setup at `zstd_dump_parser.py:36`. | Doris BE profiles, not Python profilers, determine storage/index conclusions. cProfile method/line evidence is used for Python conclusions; Scalene remains non-actionable for query-line attribution on this Windows/CPython 3.14 run. The v14 py-spy method/line trace remains the direct producer-localization evidence. |

## Recovery and profiling evidence (2026-08-06)

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| LLVM UCRT64 `llvm-dwarfutil` repair probe | `blocked` | Through the `UCRT64` profile, `llvm-dwarfutil --no-odr --no-garbage-collection --verify` ran for 37.3 seconds and wrote only to `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\llvm-dwarfutil-v1-20260806`. It returned exit 1 because output verification failed. The 800,602,848-byte output passes `--statistics`, but targeted `llvm-dwarfdump` still reports invalid abbreviation 2261 at the original `0x0802b28c`; `--verify` reports one invalid CU offset and 906 invalid DIE references. | The output is a rejected diagnostic artifact with a different SHA-256 from the source. It is not a repaired source, canonical input, or authorization for a full traversal. |
| Replacement full Parquet traversal — v15 | `rejected`, explicitly stopped | The direct-Parquet/no-JSONL/no-checkpoint run under `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\full-direct-v15-20260806` ran for approximately seven minutes, reached about 2.3 GiB RSS and 775 seconds of accumulated CPU, and had not published a manifest; open staged files were not a complete store. It was explicitly stopped before the known malformed CU because the raw-preserving fallback could only produce partial semantic output, and no accepted downstream query or header could use it. | No manifest, CU-completeness evidence, query result, or generated header came from this run. The stopped process was stale under the pre-recovery acceptance boundary. |
| Current producer method/line trace — v15 | `partial`, diagnostic | `py-spy 0.4.2` captured `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\full-direct-v15-20260806\\pyspy-worker-24156-15s.speedscope.json` with 440 samples. Leading exact leaves were `parquet.py:_flush_buffer` lines 181/184 (207), `pyarrow.parquet.core:write_table` line 1216 (26), `parquet_rows.py:_value_columns` line 388 (24), `unit_emitter.py:_value` line 252 (20), `parquet_rows.py:_scalar_value_columns` line 407 (19), `parquet_rows.py:_attribute_row` line 275 (15), JSON encoding (25), `record_sink.py:write` line 32 (12), and `unit_emitter.py:_write_attributes` lines 171/177 (19). | This is the current producer localization requested before optimization. It supports measuring batching/serialization next; it does not justify a code optimization or prove end-to-end improvement. |
| Scalene bounded producer profile — v15 | `partial`, non-actionable on Windows | Scalene 2.3.0 with `--profile-all` profiled a bounded four-CU materialization at `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\scalene-bounded-v15-all-20260806\\scalene-profile.json`. The run reached publication and hit Windows `WinError 5` during `os.replace`; `scalene view --cli` attributed 83.33% (36.507 of 43.809 seconds) to the profiler's threading worker. Application attribution was small and consistent with Parquet flushing, row conversion, and `ParquetWriter.write_table`. | Scalene is retained as required evidence, but its Windows/CPython 3.14 profiler-thread dominance and publish interference make it unsuitable for selecting a hot line. The py-spy method/line trace is the actionable producer localization. |
| Raw malformed-CU resynchronization probe | `partial`, ambiguous | A standalone pyelftools raw-stream overlay can reach the CU boundary `0x0812a18d` after three one-byte candidate repairs at `0x0802b28a`, `0x0802b29e`, and `0x0802b2b6`, yielding 140,712 remaining DIE records. The candidate byte `0x0f` is structurally plausible, but abbreviation codes 6 and 15 have indistinguishable encoded payloads at these sites (`DW_TAG_formal_parameter` with different semantic flags), so raw lookahead cannot establish the intended meaning. | No automatic repair is enabled. The canonical producer preserves raw bytes and publishes parser diagnostics, then fails closed for normal generation, knowledge export, Doris, Iceberg, and parity until source semantics are independently established. |
| Parser-diagnostic fail-closed gate | `observed` | Focused recovery tests passed 10 tests after the malformed-CU contract change. A manifest with `parse_error_count > 0` is `partial`; normal Parquet/JSONL loading, Doris planning, and Iceberg publication reject it, while `--allow-incomplete` permits diagnostic inspection only. | This protects against silently promoting an apparently complete but semantically damaged store; full-corpus traversal remains deferred until a valid recovery or source correction is available. |

## Current live-Doris explain/profile evidence (2026-08-09)

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| Current benchmark diagnostic suite | `partial`, result workloads observed | The fresh report at `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\current-doris-diagnostics-default-20260809\current-doris-benchmark.json` reused the complete source-bound manifest `store-4236f598acc8f158` and live `dwarf` publication without materialization, Stream Load, DDL, or schema changes. Report schema `1.1` records 46 distinct exact SQL statements, 92/92 `EXPLAIN` and `EXPLAIN VERBOSE` artifacts, 216 cold/warm executions, and 215 accepted profiles. | One execution returned a FE/CLI profile that did not contain its requested query ID; its raw response is retained as `partial` and was not reused. The overall report is therefore incomplete even though query results remain observed. |
| Current generate controls | `observed` | `MtObject`, `rLayout`, and exhaustive/full-hierarchy `rAIFSM` all published ordered header bundles. The rAIFSM child retained 349.470 seconds wall, 7.094 seconds user CPU, 4.844 seconds system CPU, 166,400,000-byte peak RSS, 16,868,100 bytes read, 2,212,396 bytes written, and 347 samples. | The bounded Doris contract remains first-definition/`LIMIT 1001`; it is not a complete rAIFSM hierarchy query. |
| Explain/profile artifact contract | `observed`, with explicit incomplete state | Raw and normalized plans plus raw/full profiles are external under `doris-diagnostics/statements/<statement-id>/`; each record retains source identity, schema/session context, SQL/hash, query ID, result hash, profile-fetch timing, fallback attempts, and plan/profile summaries. | The default run does not force `--no-cache` or session tuning. A cache/session mismatch is retained as diagnostic evidence rather than hidden. |

## Doris optimization evaluation implementation evidence (2026-08-09)

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| Typed serving variant and optimization report | `observed`, deterministic | `DorisServingVariant`, `DorisQueryObservation`, and `DorisOptimizationReport` now serialize source/schema/DDL/configuration identity, complete row counts, cold/warm samples, query traces, output hashes, load/statistics/tablet evidence slots, and rejected/not-applicable decisions. The focused optimization suite passes 16 tests, including exact JSON round-trip serialization and explicit unavailable-profile handling. | No live candidate is promoted by these deterministic tests. |
| Actual generation query tracing | `partial`, real attribution | The Doris query executor records parameter-free JSONL observations, query-shape digests, local execute/fetch timing, result rows, query IDs, and bounded FE-local profile artifacts. The optimized `rAIFSM` trace recorded 2,208 observations and published exact output; missing/mismatched FE profiles remain `partial`, and tracing is disabled by default. | Tracing added 160.1% wall time, so the traced run is attribution-only and excluded from performance conclusions. |
| Selective statistics policy | `observed`, SQL and live evidence | The promoted configuration defaults to selective `ANALYZE TABLE` columns with `WITH SAMPLE ROWS 4194304`, excluding raw/detail payload columns; all 14 requested live jobs reached terminal `Finished` with zero failures. Raw `SHOW TABLE STATS`, `SHOW COLUMN STATS`, `SHOW ANALYZE`, `SHOW AUTO ANALYZE`, and internal column-statistics evidence remain retained. | Four historical automatic-analysis memory-limit failures remain context; they do not invalidate the current terminal-success jobs. |
| Optimization command and candidate isolation | `observed`, implementation | `performance benchmark-doris-optimization` exposes 3/5 control and 1/3 rAIFSM screening defaults, canonical reuse, explicit candidate provisioning, source-bound name/method/DIE candidate SQL, and a 10%/110% promotion gate. CLI help and focused routes pass. | Auxiliary table provisioning, candidate load/tablet evidence, exact full-corpus parity, and confirmatory p50/p95 results are `not_observed`. |
| Canonical promotion decision | `observed`, serving algorithm retained | The canonical source-first fourteen-family model, V2/ZSTD storage, one-partition layout, replication-one deployment, indexes, and registry remain unchanged. Bounded source/unit-aware hydration is retained in the canonical runtime after exact `rLayout` and exhaustive `rAIFSM` confirmation. | No physical candidate has satisfied the full promotion gate; lookup-table, MV, index, bucket, storage-format, session, and Stream Load variants remain `not_observed` or rejected as separately recorded. |

## Recovery authorization and writer-layout probes (2026-08-06)

| Surface | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| Preserved LLVM textual dump cross-check | `observed` | Through the `UCRT64` profile, streaming `zstd -dc` over `D:\\research\\DDON-binaries\\IDA9.3\\PS4_DDON_02020005_2016_12_21\\DDOORBIS.elf.llvmdwarfdump.zst` found the same CU `0x07fdc9b7`, boundary `0x0812a18d`, and three records at `0x0802b28a`, `0x0802b29e`, and `0x0802b2b6` as `DW_TAG_formal_parameter [15]`; the recorded `DW_FORM_ref4` values are `0x9111`, `0xadb7`, and `0xadb7`. | This is independent textual LLVM evidence for the source-bound repair profile, not a replacement source ELF or a generic malformed-DWARF recovery algorithm. |
| Source-bound malformed-CU recovery overlay | `observed`, bounded | For source SHA-256 `4236f598acc8f15893181455ed195e39dfa4dbfda4eeda8b56fcbd82312c63c0`, six guarded substitutions restore the three LLVM-recorded DIEs in memory while raw-section files retain original bytes and hashes. pyelftools then iterated 188,272 DIEs to the exact CU boundary; the analytical emitter produced 1,197,632 rows across ten closed Zstandard Parquet files, and every row group read successfully. | The profile is deliberately source-bound and fails on a context mismatch. Full-corpus zero-error, row-count, header, and query parity evidence remains pending. |
| Bounded recovery materialization — v16 | `partial`, diagnostic | `bounded-recovery-v16-20260806` with `--max-cus 1` published ten closed Parquet artifacts. Its manifest records `producer=pyelftools-0.33-one-pass-direct-source-recovery`, `dwarf_recovery.status=applied`, `parse_error_count=0`, and `status=partial` solely because the bounded request stopped after one CU. | This validates manifest integration and the first-CU path; it cannot establish complete-CU coverage or serve generation. |
| Replacement full Parquet traversal — v17 | `rejected`, native writer crash | Started at 2026-08-06 15:15:37 CEST under `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\full-direct-v17-20260806` with direct typed Parquet, Zstandard, no JSONL, and no checkpoint. Windows Error Reporting recorded an `APPCRASH` at 15:34:35 in `pyarrow\\lib.cp314-win_amd64.pyd` with access violation `0xc0000005`; the process exited without a manifest. The staging directory retained 52 non-footer Parquet files and 1,144,051,386 bytes, so no rows were promoted. | This is a native pyarrow writer-lifecycle failure, not accepted store evidence. The open-writer default was reduced from 64 to 16, exposed as `--max-open-writers`, and recorded in the materialization configuration. A bounded multi-bucket probe must pass before another full traversal. |
| Replacement bounded recovery probe — v17 writer bound | `partial`, observed | Started at 2026-08-06 15:39:16 CEST under `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\bounded-recovery-v17-w16-64-20260806` with `--max-cus 64` and `--max-open-writers 16`; it published at 15:45:23 with 64 CUs, 3,679,663 DIEs, 22 closed Zstandard Parquet files, 148,540,678 Parquet bytes, zero parser errors, recovery `applied`, `peak_open_writers=16`, and six automatic rotations. `inspect-dwarf-store --allow-incomplete` verified every declared hash, footer, row group, and compressed payload. | This validates the writer bound and multi-bucket publication path, but remains diagnostic evidence because it stops after 64 of 2,305 CUs. |
| Replacement full Parquet traversal — v18 | `rejected`, native crash | Started at 2026-08-06 15:47:26 CEST under `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\full-direct-v18-20260806` with direct typed Parquet, Zstandard, no JSONL, no checkpoint, and `--max-open-writers 16`. It crossed the v17 crash boundary and reached 192 staged files / 1,828,389,787 bytes before Windows Error Reporting recorded `python.exe` faulting in `D:\\ddon-dwarf-reconstructor\\.venv\\Lib\\site-packages\\pyarrow\\lib.cp314-win_amd64.pyd` at 17:04:12 with access violation `0xc0000005` (process 31944). No manifest was published; 176 files had readable footers and 16 open files were truncated or lacked footer magic. | This repeats the native PyArrow failure under the reduced 16-writer bound. The staged prefix is not queryable or loadable; family/source layout alone is insufficient. |
| One-writer bounded recovery probe — v19 | `rejected`, pathological file churn | The diagnostic `bounded-recovery-v19-w1-64-20260806` used `--max-open-writers 1` and was explicitly stopped after it created approximately 59,077 Parquet files / 357,973,414 bytes in under a minute because interleaved record families forced constant rotation. | A low writer count is not a safe optimization. Retain the diagnostic output for overhead evidence, but do not use it for queries, Doris, Iceberg, or acceptance. |
| Bounded family/source recovery probe — v20 | `partial`, observed | The real-ELF `bounded-family-v20-64-20260806` probe used the pre-rotation family layout, 64 CUs, direct Zstandard Parquet, no JSONL, and `--max-open-writers 16`. It published 12 closed family/source files, 380 row groups, 148,600,334 Parquet bytes, 3,679,663 DIEs, 10,518,365 attributes, 1,368,157 index rows, 1,356,640 names, zero parser errors, `peak_open_writers=12`, and zero rotations. `inspect-dwarf-store --allow-incomplete` validated every declared hash, footer, row group, and compressed payload; independent Arrow reads confirmed the physical `unit_bucket` column and source-only directory paths. | This validates a 64-CU family slice only. The v21 result shows that its long-lived writers are not safe for a full run; the new CU-boundary rotation must pass a bounded real-ELF probe first. |
| Replacement full Parquet traversal — v21 family layout | `rejected`, native crash | Started at 2026-08-06 17:42:00 CEST under `C:\\Users\\morph\\AppData\\Local\\Temp\\ddon-analytical-dwarf\\full-direct-v21-family-20260806` with direct typed Parquet, Zstandard, no JSONL, no checkpoint, family layout, and `--max-open-writers 16`. It terminated at 19:34:41 after about 112 minutes with the repeated `pyarrow\\lib.cp314-win_amd64.pyd` access violation (`0xc0000005`, offset `0x16a8f0`, process 22356). The 12 staged Parquet files totalled 2,654,381,412 bytes; the largest attribute file was 1,345,961,777 bytes, all 12 files lacked readable footers, and no manifest was published. | This rejects family/source layout without bounded CU rotation. The staged files are not queryable or loadable. The next full attempt requires the new rotation behavior and a fresh bounded real-ELF validation. |
| CU-boundary Parquet rotation implementation | `observed`, deterministic | The sink closes family writers at a configurable CU interval (default 64) without publishing a checkpoint, continues with numbered parts, and records `cu_boundary_rotations` separately. Focused analytical tests passed 26 tests, including two readable family files after one explicit rotation and request validation for the new interval. | Real-ELF rotation overhead, closed-file count, and complete-manifest evidence remain pending. |
| Bounded family/source rotation probe — v22 | `partial`, observed | `bounded-family-rotation-v22-64-20260806` used the production family layout, direct Zstandard Parquet, no JSONL, `--max-open-writers 16`, `--rotate-writers-every-cus 64`, and `--max-cus 64`. It completed in 384 seconds with 64 CUs, 12 closed files, 380 row groups, zero parser errors, `peak_open_writers=12`, `automatic_rotations=0`, and `cu_boundary_rotations=1`; `inspect-dwarf-store --allow-incomplete` validated all declared hashes, footers, row groups, and compressed payloads. | This validates the new writer lifecycle but remains partial because only 64 of 2,305 CUs were traversed. It authorizes one full run with the same rotation configuration; no runtime, Doris, Iceberg, or header parity evidence comes from this slice. |
| Replacement full Parquet traversal — v23 family rotation | `rejected`, Python runtime crash | `full-direct-v23-family-rotate64-20260806` started at 2026-08-06 19:51:47 CEST with direct typed Zstandard Parquet, no JSONL/checkpoint, family layout, `--max-open-writers 16`, and `--rotate-writers-every-cus 64`. It stopped at 21:13:55 CEST; WER recorded process 20412 (`python.exe`, CPython 3.14.6) faulting in `python314.dll` at offset `0x11b5e9` with access violation `0xc0000005`. Read-only footer inspection found 162 Parquet files: 152 readable files covering 272,002,245 rows and 4,490 row groups, and ten invalid `part-00015` files (one per family); the directory had 173 files and 2,701,475,635 total bytes. No manifest was published. | The new CU-boundary rotation closed repeated parts successfully, so the failure is no longer the earlier long-lived `pyarrow` footer-loss signature; however, the CPython/native analytical path remains unreliable over the full run. The staged prefix is not queryable, loadable, or completeness evidence. A bounded crash-isolation and profiler-led runtime/native-boundary probe is required before another full traversal. |
| Canonical materializer profiler loop — v24 | `observed`, bounded and partial by tool | `performance profile-materializer` ran independent 8-CU targets under cProfile, Scalene 2.3.0, and py-spy. cProfile was observed at 135.004 s wall, 131.203 s user CPU, 4.063 s system CPU, and 1,609,420,800-byte peak RSS; its pstats self-time leaders were `parquet.py:_flush_buffer` (11.548 s), `parquet_rows.py:_value_columns` (9.095 s), `parquet_rows.py:_attribute_row` (7.614 s), `unit_emitter.py:_write_attributes` (4.983 s), and `parquet.py:write` (7.243 s self / 83.911 s cumulative). Scalene produced a 6,845,543-byte artifact and process metrics but exited 1; 88.518% of its sampled CPU was `<eval@__init__.py:447>`, so no application hot-line conclusion is accepted. The repository py-spy wrapper exited before sampling; a manual `py-spy 0.4.2` attach to the same unprofiled 8-CU target wrote `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\pyspy-materializer-v24-8-20260806\pyspy.speedscope.json` with 749 samples and zero errors, with exact leaves led by `parquet.py:_flush_buffer` (203 combined samples), `parquet_rows.py:_value_columns` (52), `_scalar_value_columns` (51), `dwarf_recovery.py:read` (27), `pyarrow.parquet.write_table` (17), and `unit_emitter.py:_value` (16). | This provides the requested method/line CPU localization and keeps Scalene's limitation explicit. The targets are partial 8-CU stores and cannot establish full-corpus throughput, reliability, parity, or an optimization win. |
| Bounded CU-rotation stress — v25 | `observed`, partial and payload-validated | `bounded-family-rotation-v25-256-r16-20260806` completed normally after 256 CUs with direct typed Zstandard Parquet, no JSONL, family/source layout, `--max-open-writers 16`, and `--rotate-writers-every-cus 16`. Its source-bound manifest records 12,787,238 DIEs, 36,300,455 attributes, 18,914,069 references, 4,752,377 index rows, 4,708,022 names, 256 units, zero parser errors, 16 CU-boundary rotations, `peak_open_writers=12`, 162 Parquet files, 1,399 row groups, and 79,716,446 rows. An independent Arrow batch read of every column completed without errors in 3.436 seconds; all codecs were ZSTD and the files totalled 526,580,562 bytes. | This materially validates repeated writer close/reopen and payload integrity across four times the v22 CU interval, and it did not reproduce the v18/v21/v23 crash path. The manifest is intentionally `partial` because `--max-cus 256` stopped before all 2,305 CUs; the store remains diagnostic-only and cannot serve generation, knowledge export, Doris, Iceberg, or completeness evidence. |
| Bounded v25 query contract | `observed`, partial diagnostic | `performance benchmark-dwarf-store --allow-incomplete` loaded the v25 manifest source-bound and queried the file projection. The `rLayout` prefix returned 17 definitions, while the same run reported `partial` for all analytical query results; the report is `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\query-bounded-v25-20260806\benchmark-report.json`. File lookup was a 162-file/1,399-row-group scan, with a 0.041-second cold and 0.038-second warm definition query in the diagnostic process. | The 17 matches are only the first 256 CUs and cannot be compared as complete `rLayout` parity. No generated header, knowledge export, Doris load, or backend acceptance may consume this report. |
| Bounded CU-rotation stress — v26 | `observed`, partial and payload-validated | `bounded-family-rotation-v26-1024-r16-20260806` completed normally with `PYTHONFAULTHANDLER=1` after 1,024 CUs using direct typed Zstandard Parquet, no JSONL, family/source layout, `--max-open-writers 16`, and `--rotate-writers-every-cus 16`. The source-bound manifest records 47,292,085 DIEs, 133,994,692 attributes, 69,926,012 references, 17,482,544 index rows, 17,271,294 names, 1,024 units, zero parser errors, 64 CU-boundary rotations, `peak_open_writers=12`, 642 Parquet files, 5,190 row groups, 294,968,826 rows, and 1,960,707,231 Parquet bytes. `inspect-dwarf-store --allow-incomplete` exited 0; an independent Arrow read of every column verified all 642 files in 12.776 seconds with no row mismatch or payload error, and all codecs were ZSTD. No new WER dump was created. | This is the cumulative reliability gate for the 16-CU rotation and direct native path, not a complete store: 1,024 of 2,305 CUs were traversed. It cannot serve generation, knowledge export, Doris, Iceberg, or completeness/header parity evidence. |
| Bounded v26 query contract | `observed`, partial diagnostic | `performance benchmark-dwarf-store --allow-incomplete` loaded the v26 manifest and returned 96 `rLayout` definitions from the 1,024-CU prefix. The report is `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\query-bounded-v26-20260806\benchmark-report.json`; the file definition query took 2.663 seconds cold and 0.434 seconds warm, scanning the 642-file projection. | The 96 matches demonstrate increased prefix coverage but are not complete `rLayout` parity; the benchmark explicitly reports `partial` and the store remains barred from generation, knowledge export, Doris, Iceberg, and acceptance. |
| Windows crash-dump triage — v18/v21/v23 | `observed`, diagnostic | Windows Error Reporting retained `C:\Users\morph\AppData\Local\CrashDumps\python.exe.31944.dmp`, `python.exe.22356.dmp`, and `python.exe.20412.dmp`. CDB 10.0.22621.755 `!analyze -v` found the same invalid-pointer read at `lib.cp314_win_amd64+0x16a8f0` in v18 and v21, with callers in `arrow_python!arrow::py::ConvertPySequence`; v23 instead faulted at `python314!Py_HandlePending+0x11b9` while `pyarrow`/Arrow modules were loaded. Reusable CDB logs are under `C:\Users\morph\AppData\Local\Temp\ddon-analytical-dwarf\windows-dumps`. | These are WER user mini-dumps, so heap/object state is incomplete and the shared native signature does not prove the ultimate corrupting write. The effective system WER policy is the existing `CrashDumps` directory; HKLM per-app full-dump configuration was denied and HKCU `LocalDumps` is unsupported by Microsoft. Future bounded materializer profiler children set `PYTHONFAULTHANDLER=1`; use CDB/WinDbg evidence before another full traversal. |
